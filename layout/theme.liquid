<html>
    <head>
        {{ content_for_header  }}
        {% comment %} bootstrap {% endcomment %}
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-QWTKZyjpPEjISv5WaRU9OFeRpok6YctnYmDr5pNlyT2bRjXh0JMhjY6hW+ALEwIH" crossorigin="anonymous">
        <link rel="stylesheet" href="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/css/bootstrap.min.css">
        <script src="https://ajax.googleapis.com/ajax/libs/jquery/3.7.1/jquery.min.js"></script>
        <script src="https://maxcdn.bootstrapcdn.com/bootstrap/3.4.1/js/bootstrap.min.js"></script>
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.0.2/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-EVSTQN3/azprG1Anm3QDgpJLIm9Nao0Yz1ztcQTwFspd3yD65VohhpuuCOmLASjC" crossorigin="anonymous">
        
        <style>

            /* hide an image on mobile */
            @media screen and (max-width: 600px) {
                #next {
                    display: none;
                }
            }

            /* The Modal (background) */
            .modal {
            display: none; /* Hidden by default */
            position: fixed; /* Stay in place */
            z-index: 2; /* Sit on top */
            /* padding-top: 100px; Location of the box */
            /* left: 0; */
            /* top: 0; */
            width: 30%; Full width
            /* height: 50%; */
            /* height: 10%; Full height */
            overflow: auto; /* Enable scroll if needed */
            /* background-color: rgb(0,0,0); Fallback color */
            /* background-color: rgba(0,0,0,0.4); Black w/ opacity */
            /* display: flex; */
            justify-content: center; /* Center horizontally */
            align-items: center; /* Center vertically */
            left:35% ;
            top:15%;
            

            }

            /* Modal Content */
            .modal-content {
            background-color: #fefefe;
            margin: auto;
            padding: 20px;
            border: 2px solid red;
            /* width: 0%; */
            }

            /* The Close Button */
            .close {
            color: #aaaaaa;
            float: right;
            font-size: 28px;
            font-weight: bold;
            }

            .close:hover,
            .close:focus {
            color: #000;
            text-decoration: none;
            cursor:auto;
            }
           /* increase size of cirlce and + sign*/
            .plus-circle {
                position: absolute;
                top: 50%;
                left: 50%;
                transform: translate(-50%, -50%); /* Centering the circle */
                width: 20px; /* Adjust the size of the circle */
                height: 20px; /* Adjust the size of the circle */
                background-color: white; /* Optional: background color of the circle */
                border: 5px solid white;
                border-radius: 50%;
                display: flex;
                align-items: center;
                justify-content: center;
                font-size: 24px; /* Adjust the size of the + symbol */
                /* font-weight: bold; */
                color: black; /* Optional: color of the + symbol */
            }

            /* for shopping button  */
            /* Styling the shop button */
        .shop-button {
            width:auto; /* Set the size of the square button */
            height: 30px; /* Set the size of the square button */
            background-color: yellow; /* Initial background color */
            color: black; /* Text color */
            border: none; /* Remove the default border */
            font-size: 18px; /* Text size */
            font-weight: bold; /* Make text bold */
            cursor: pointer; /* Change cursor to pointer on hover */
            position: relative; /* Position relative for hover effect */
            overflow: hidden; /* Hide overflow for smooth transition */
            transition: color 0.3s ease; /* Smooth text color transition */
        }

        .shop-button::before {
            content: 'Choose Gift->';
            position: absolute;
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            z-index=1;
            background: linear-gradient(to right, yellow 0%, black 100%); /* Gradient from yellow to black */
            transition: left 0.3s ease; /* Smooth transition for hover effect */
        }

        .shop-button:hover::before {
            left: 0; /* Move gradient from left to right on hover */
        }

        .shop-button:hover {
            color: white; /* Change text color on hover */
        }

        /* button second  */
        .shop-button1 {
            width:200px; /* Set the size of the square button */
            height: 50px; /* Set the size of the square button */
            background-color: black; /* Initial background color */
            color:white; /* Text color */
            border: none; /* Remove the default border */
            font-size: 18px; /* Text size */
            font-weight: bold; /* Make text bold */
            cursor: pointer; /* Change cursor to pointer on hover */
            position: relative; /* Position relative for hover effect */
            overflow: hidden; /* Hide overflow for smooth transition */
            transition: color 0.3s ease; /* Smooth text color transition */
        }

        .shop-button1::before {
            content: 'SHOP NOW->';
            position: absolute;
            display: flex; /* Use flexbox */
            align-items: center; /* Center vertically */
            justify-content: center; /* Center horizontally */
            top: 0;
            left: -100%;
            width: 100%;
            height: 100%;
            z-index=1;
            background: linear-gradient(to right, black 0%, yellow 100%); /* Gradient from yellow to black */
            transition: left 0.3s ease; /* Smooth transition for hover effect */
        }

        .shop-button1:hover::before {
            left: 0; /* Move gradient from left to right on hover */
        }

        .shop-button1:hover {
            color: white; /* Change text color on hover */
        }
        </style>
        </head>
    <body>
        {{ content_for_layout  }}
    </body>
    <script>
           document.addEventListener('DOMContentLoaded', function() {
    // Get all modal buttons
    var modalButtons = document.querySelectorAll('.modal-btn');

    modalButtons.forEach(function(button) {
        button.addEventListener('click', function() {
            var modalId = button.getAttribute('data-modal');
            var modal = document.getElementById(modalId);
            modal.style.display = "block";
        });
    });

    // Get all close buttons
    var closeButtons = document.querySelectorAll('.close');

    closeButtons.forEach(function(button) {
        button.addEventListener('click', function() {
            var modalId = button.getAttribute('data-modal');
            var modal = document.getElementById(modalId);
            modal.style.display = "none";
        });
    });

    // Close the modal when clicking outside of it
    window.onclick = function(event) {
        modalButtons.forEach(function(button) {
            var modalId = button.getAttribute('data-modal');
            var modal = document.getElementById(modalId);
            if (event.target == modal) {
                modal.style.display = "none";
            }
        });
    };
});
        
    </script>

</html>